# TASK 3: Write this prompt file.
#
# This prompt receives three data inputs and produces a markdown report.
#
# Your prompt must:
#   1. Declare Claude's role clearly (FinTrack engineering intelligence system)
#   2. Pass in the three data inputs (PRs, issues, DB alerts)
#   3. Instruct Claude to produce output with EXACTLY these section headers:
#        ## PRs_NEEDING_REVIEW
#        ## OPEN_P0_P1
#        ## OVERNIGHT_DB_ALERTS
#        ## ACTION_ITEMS  (exactly 3 bullet points, most urgent first)
#   4. Include a rule: if any data source returned empty, show the header with
#      "No data returned from [source]"
#   5. Include a rule: no PII, no raw SQL, no customer names
#
# Replace this entire comment block with your actual prompt.
# Use {{PR_DATA}}, {{ISSUE_DATA}}, {{DB_ALERTS}} as placeholders for the data
# that morning_brief.py will inject before calling mcp.ask().
